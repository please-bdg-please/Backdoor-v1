platform :ios, '15.0'

target 'backdoor' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for backdoor - Only include the packages that were causing issues with SPM
  pod 'CryptoSwift', '~> 1.8.3'
  pod 'SnapKit', '~> 5.0.1'
  pod 'lottie-ios', '~> 4.5.1'
  pod 'Moya', '~> 15.0.0'
  pod 'R.swift', '~> 7.0.0'
  
  # Note: SwiftUIX should still be added via SPM as it doesn't have a CocoaPods spec
end
